name: mlflow-linearml

python_env: python_env.yaml
# or
# conda_env: conda_env.yaml

entry_points:
  main:
    parameters:
      alpha: {type: float, default: 0.5}
      l1_ratio: {type: float, default: 0.1}
    command: "python main.py {alpha} {l1_ratio}"